---
import { Image } from 'astro:assets';
const { normalImage, hoverImage } = Astro.props;
---

<!-- <div class="icon-box">
  <img src={normalImage} alt="Normal Logo" class="logo normal" />
  <img src={hoverImage} alt="Hover Logo" class="logo hover" />
</div> -->
<div class="icon-box">
  <Image
    src={normalImage}
    alt="Normal Logo"
    class="logo normal"
    width={200}       
    height={100}      
  />
  <Image
    src={hoverImage}
    alt="Hover Logo"
    class="logo hover"
    width={200}       
    height={100}
  />
</div>


<style>
.icon-box {
  display: inline-block;
  padding: 10px;
  border-radius: 5px; /* Rounded corners */
  border: 2px solid black; /* Black outline */
  background-color: transparent; /* Clear inside */
  transition: background-color 0.3s ease;
  position: relative; /* Set as the positioning reference */
  width: 50px; 
  height: 50px;
  cursor: pointer;
}

.logo {
  width: 25px; /* Set appropriate size */
  height: 25px;
  position: absolute;
  top: 50%; /* Center vertically */
  left: 50%; /* Center horizontally */
  transform: translate(-50%, -50%); /* Offset the logo by half its size to center it */
  transition: opacity 0.3s ease;
}

.logo.hover {
  opacity: 0; /* Initially hide the hover logo */
}

.icon-box:hover .logo.hover {
  opacity: 1; /* Show hover logo on hover */
}

.icon-box:hover .logo.normal {
  opacity: 0; /* Hide normal logo on hover */
}

.icon-box:hover {
  background-color: black; /* Black background on hover */

}

.icon-box:hover .logo.hover {
  opacity: 1; /* Show hover logo on hover */
}

.icon-box:hover .logo.normal {
  opacity: 0; /* Hide normal logo on hover */
}

</style>
